# These files will end up in the install include directory
# For example:
#   * with prefix include_ the files will be install to /usr/include (in Debian)
#   * with prefix pkginclude_ the files will be install to /usr/include/pkgname (in Debian)
include_HEADERS = \
  libknxusb.h
